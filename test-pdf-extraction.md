# اختبار استخراج النص من PDF

هذا الملف يحتوي على تعليمات لاختبار آلية استخراج النص الجديدة من ملفات PDF.

## التحسينات المطبقة:

### 1. استبدال مكتبة pdf-parse بـ pdfjs-dist
- مكتبة أكثر دقة وموثوقية
- دعم أفضل للنصوص العربية
- معالجة أفضل للترميز

### 2. تنظيف النص المستخرج
- إزالة الرموز المعطوبة (\uFFFD)
- إزالة رموز التحكم غير المرغوب فيها
- توحيد المسافات والحفاظ على الفقرات

### 3. معالجة أفضل للأخطاء
- رسائل خطأ واضحة باللغة العربية
- التحقق من حجم الملف (حد أقصى 50 ميجابايت)
- التحقق من وجود نص كافٍ للمعالجة

### 4. استخراج النص صفحة بصفحة
- معالجة كل صفحة منفصلة
- تجميع النص بالتسلسل الصحيح
- الحفاظ على بنية المستند

## كيفية الاختبار:

1. ارفع ملف PDF يحتوي على نص عربي أو إنجليزي
2. تأكد من أن النص يتم استخراجه بشكل صحيح
3. تحقق من أن التنسيق والفقرات محفوظة
4. اختبر ملفات PDF مختلفة الأحجام والأنواع

## الملفات المحدثة:

- `src/app/api/extract-pdf/route.ts` - API endpoint محدث
- `package.json` - إزالة pdf-parse وإضافة pdfjs-dist

## النتيجة المتوقعة:

يجب أن تعمل آلية استخراج النص من PDF بنفس دقة وبساطة ملفات TXT، مع نص نظيف وواضح قابل للعرض والمعالجة والبحث.